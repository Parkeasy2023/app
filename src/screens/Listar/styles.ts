import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 10
    },
    title: {
        textAlign: "center",
        fontWeight: "bold",
        fontSize: 20,
        padding: 5
    }
})